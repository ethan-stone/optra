meta {
  name: Create Client
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/internal.createRootClient
  body: json
  auth: none
}

auth:bearer {
  token: wef
}

body:json {
  {
    "name": "wefwef"
  }
}
