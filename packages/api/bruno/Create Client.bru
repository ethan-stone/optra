meta {
  name: Create Client
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/internal.createRootClient
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "name": "test client"
  }
}
