meta {
  name: Add Api Scope
  type: http
  seq: 6
}

post {
  url: {{base_url}}/v1/apis.addScope
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "apiId": "api_HV7nGnFrMcMGJqnBMP3gE",
    "scope": {
      "name": "write:file",
      "description": "Allows clients to write files"
    }
  }
}
