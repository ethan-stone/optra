meta {
  name: Create Workspace
  type: http
  seq: 2
}

post {
  url: {{base_url}}/v1/internal.createWorkspace
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "name": "wefwef"
  }
}
