meta {
  name: Create Api
  type: http
  seq: 3
}

post {
  url: {{base_url}}/v1/apis.createApi
  body: json
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
    "name": "test api"
  }
}
